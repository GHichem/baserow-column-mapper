{
  "name": "baserow-file-proxy",
  "version": "1.0.0",
  "description": "Proxy server to handle CORS issues when fetching files from Baserow",
  "main": "proxy-server.js",
  "scripts": {
    "start": "node proxy-server.js",
    "dev": "nodemon proxy-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "keywords": [
    "baserow",
    "proxy",
    "cors",
    "file-download"
  ],
  "author": "",
  "license": "MIT"
}
